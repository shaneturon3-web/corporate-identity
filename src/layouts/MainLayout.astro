---
/** Institutional root layout — Order 003 will apply grid + tokens. */
interface Props {
  title?: string;
}
const { title = "Shane Turon" } = Astro.props;
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <slot name="head" />
  </head>
  <body>
    <slot />
  </body>
</html>
